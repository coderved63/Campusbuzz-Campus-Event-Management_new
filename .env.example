# Environment Variables Configuration

# MongoDB Connection String (from your existing API .env)
MONGODB_URI=mongodb://localhost:27017/campusbuzz

# JWT Secret for authentication tokens (matches your API's jwtSecret)
JWT_SECRET=bsbsfbrnsftentwnnwnwn

# Next.js Environment
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# Admin Secret for creating admin users (from your existing API .env)
ADMIN_SECRET=evento_admin_2024_secret

# API Port (from your existing API configuration)
PORT=4000

# Upload Configuration
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=jpeg,jpg,png,gif,webp,avif

# Development URLs (matching your existing client-server setup)
API_BASE_URL=http://localhost:4000
CLIENT_BASE_URL=http://localhost:3000